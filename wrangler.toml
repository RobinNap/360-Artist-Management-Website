# Simple Cloudflare Pages Configuration
# This file is optional - Cloudflare will auto-detect your static files
# For a simple static site (like GitHub Pages), minimal config is best

name = "360-artist-management-website"
compatibility_date = "2024-01-01"
pages_build_output_dir = "./"  # Directory containing static files (root directory)

# Note: R2 bindings removed - not needed for simple static site
# If you need R2 later, you can add bindings in Cloudflare Dashboard




