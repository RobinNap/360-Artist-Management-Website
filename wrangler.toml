# Cloudflare Workers Configuration
# This Worker serves static files (HTML, CSS, JS) from the repository

name = "360-artist-management-website"
compatibility_date = "2025-12-23"
main = "worker.js"

# Static assets configuration - Workers will serve files from this directory
[site]
bucket = "./"




