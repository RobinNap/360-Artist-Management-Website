# Cloudflare Workers & Pages Configuration (Unified Platform)
# This configures static asset serving in the unified Workers & Pages platform
# For Git-connected projects, deployments happen automatically

name = "360-artist-management-website"
compatibility_date = "2025-12-23"

# Static assets configuration for unified platform
pages_build_output_dir = "./"  # Root directory - all static files are here

# Note: In the unified Workers & Pages platform, static files are served automatically
# from the repository root when Git is connected. No Worker code needed for static sites.




